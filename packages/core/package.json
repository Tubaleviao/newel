{
  "name": "@quoin/core",
  "version": "0.0.1",
  "description": "Core IR types, builder DSL, generator runner, and CLI for quoin",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "bin": {
    "quoin": "dist/cli/index.js"
  },
  "scripts": {
    "build": "tsc",
    "test": "node --experimental-vm-modules ../../node_modules/.bin/jest --testPathPattern=src",
    "lint": "tsc --noEmit"
  },
  "dependencies": {
    "commander": "^12.0.0"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "jest": "^29.0.0",
    "@types/jest": "^29.0.0",
    "ts-jest": "^29.0.0",
    "typescript": "^5.4.0"
  }
}
