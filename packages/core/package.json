{
  "name": "@newel/core",
  "version": "0.0.1",
  "description": "Core IR types, builder DSL, generator runner, and CLI for newel",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "bin": {
    "quoin": "dist/cli/index.js"
  },
  "scripts": {
    "build": "tsc",
    "test": "NODE_OPTIONS=--experimental-vm-modules jest --testPathPattern=src",
    "lint": "tsc --noEmit"
  },
  "dependencies": {
    "commander": "^12.0.0",
    "tsx": "^4.23.0"
  },
  "devDependencies": {
    "@types/jest": "^29.0.0",
    "@types/node": "^20.0.0",
    "jest": "^29.0.0",
    "ts-jest": "^29.0.0",
    "typescript": "^5.4.0"
  }
}
